# Goal
Reduce unnecessary parsing code by storing the node number/id in state

# Example

pkg/cluster/types.go:20-37 `config.Node` is created with id in the string

pkg/cluster/types.go:39-56 parseClientNodeNumber(...) extracts the id from the string
